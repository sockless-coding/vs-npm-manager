<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">
  <Border Background="{DynamicResource {x:Static colors:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
          TextElement.Foreground="{DynamicResource {x:Static colors:EnvironmentColors.ToolWindowTextBrushKey}}"
          Padding="8">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,6">
        <Button Content="Refresh"
                MinWidth="70"
                MinHeight="24"
                IsEnabled="{Binding NotBusy}"
                Command="{Binding RefreshCommand}"
                Style="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}" />
        <TextBlock Text="{Binding Scope}" Margin="10,0,0,0" VerticalAlignment="Center" Opacity="0.8" />
      </StackPanel>

      <TextBlock Grid.Row="1" Text="{Binding Status}" Margin="0,0,0,6" TextWrapping="Wrap" />

      <ListBox Grid.Row="2" ItemsSource="{Binding Packages}">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Grid Margin="2" HorizontalAlignment="Stretch">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                <TextBlock Text="{Binding Badges}" Opacity="0.7" FontSize="11" />
              </StackPanel>
              <TextBlock Grid.Column="1"
                         Text="{Binding VersionLabel}"
                         VerticalAlignment="Center"
                         Margin="8,0,0,0"
                         FontFamily="Consolas" />
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Grid>
  </Border>
</DataTemplate>
